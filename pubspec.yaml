name: ca_riverpod_memo
description: Clean Architecture with riverpod.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  flutter_riverpod: ^1.0.1
  go_router: ^4.2.7
  path: ^1.8.1
  sqflite: ^2.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true